# Minimal constraints for test-only dependencies
# Use: pip install -c constraints-test.txt pytest httpx tenacity loguru pyyaml click
# Generated: 2026-01-04

# Test framework
pytest>=7.4.0,<9.0.0
pytest-asyncio>=0.21.0,<1.0.0
pytest-mock>=3.12.0,<4.0.0

# HTTP client (required by adapters)
httpx>=0.24.0,<1.0.0

# Retry logic (required by SafeClient)
tenacity>=8.2.0,<9.0.0

# Logging (required by adapters)
loguru>=0.7.0,<1.0.0

# Config parsing (required by setup scripts)
pyyaml>=6.0.0,<7.0.0

# CLI framework (required by setup wizard)
click>=8.1.0,<9.0.0

# Data validation (optional, for protocol checks)
pydantic>=2.0.0,<3.0.0

# Async support
anyio>=3.7.0,<5.0.0
