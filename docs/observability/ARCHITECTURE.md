# Observability Architecture

Visual architecture guide for CUGAR agent observability system.

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                      CUGAR Agent System                         │
│                                                                 │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐   │
│  │ Planner  │   │ Routing  │   │  Tools   │   │  Budget  │   │
│  │  Agent   │   │Authority │   │ Executor │   │ Enforcer │   │
│  └────┬─────┘   └────┬─────┘   └────┬─────┘   └────┬─────┘   │
│       │              │              │              │           │
│       │ PlanEvent    │ RouteEvent   │ ToolCallEvent│ BudgetEvent
│       │              │              │              │           │
│       └──────────────┴──────────────┴──────────────┘           │
│                             │                                   │
│                             ▼                                   │
│                 ┌───────────────────────┐                      │
│                 │ ObservabilityCollector│                      │
│                 │  - Thread-safe buffer │                      │
│                 │  - Auto signal update │                      │
│                 │  - Multi-exporter     │                      │
│                 └───────────┬───────────┘                      │
│                             │                                   │
└─────────────────────────────┼───────────────────────────────────┘
                              │
                ┌─────────────┼─────────────┐
                │             │             │
                ▼             ▼             ▼
        ┌──────────┐  ┌──────────┐  ┌──────────┐
        │   OTEL   │  │ Console  │  │Prometheus│
        │ Exporter │  │ Exporter │  │  /metrics│
        └────┬─────┘  └────┬─────┘  └────┬─────┘
             │             │             │
             ▼             ▼             ▼
      ┌──────────┐  ┌──────────┐  ┌──────────┐
      │  Jaeger  │  │  Stdout  │  │ Grafana  │
      │  Zipkin  │  │   Logs   │  │Dashboard │
      │   OTLP   │  └──────────┘  └──────────┘
      └──────────┘
```

## Event Flow

```
User Request
     │
     ▼
┌─────────────────┐
│ PlannerAgent    │──→ emit(PlanEvent(trace_id, goal, steps, tools, duration))
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│RoutingAuthority│──→ emit(RouteEvent(trace_id, agent, policy, alternatives))
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ ToolExecutor    │──→ emit(ToolCallEvent.start(trace_id, tool, params))
│                 │    try { execute() }
│                 │──→ emit(ToolCallEvent.complete(trace_id, tool, duration))
│                 │    catch { emit(ToolCallEvent.error(trace_id, tool, error)) }
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ BudgetEnforcer  │──→ if approaching: emit(BudgetEvent.warning(...))
│                 │──→ if exceeded: emit(BudgetEvent.exceeded(...))
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ObservabilityCol │
│     lector      │
└────────┬────────┘
         │
         ▼
   Golden Signals
```

## Golden Signals Data Flow

```
┌──────────────────────────────────────────────────┐
│              Structured Events                   │
│  - PlanEvent (steps_count, duration_ms)         │
│  - RouteEvent (duration_ms)                     │
│  - ToolCallEvent (duration_ms, error_type)      │
│  - BudgetEvent (spent, ceiling, utilization)    │
│  - ApprovalEvent (wait_time_ms)                 │
└────────────────────┬─────────────────────────────┘
                     │
                     ▼
        ┌────────────────────────┐
        │ _update_signals()      │
        │ - Map event → recorder │
        └────────────┬───────────┘
                     │
        ┌────────────┴────────────┐
        │                         │
        ▼                         ▼
┌──────────────┐        ┌──────────────┐
│   Counters   │        │  Histograms  │
├──────────────┤        ├──────────────┤
│ total_reqs   │        │ end_to_end   │
│ success_reqs │        │   latency    │
│ failed_reqs  │        │              │
│ tool_calls   │        │ planning     │
│ tool_errors  │        │  latency     │
│ budget_warn  │        │              │
│ budget_exceed│        │ routing      │
│ approval_reqs│        │  latency     │
└──────────────┘        │              │
                        │ tool_latency │
                        │  (per tool)  │
                        │              │
                        │ approval     │
                        │  wait_time   │
                        └──────────────┘
                        
                ┌────────────┴────────────┐
                │                         │
                ▼                         ▼
        ┌──────────────┐        ┌──────────────┐
        │ Calculations │        │   Export     │
        ├──────────────┤        ├──────────────┤
        │success_rate()│        │to_dict()     │
        │error_rate()  │        │              │
        │tool_error_   │        │to_prometheus_│
        │  rate()      │        │  format()    │
        │mean_steps_   │        │              │
        │  per_task()  │        │              │
        │requests_per_ │        │              │
        │  second()    │        │              │
        └──────────────┘        └──────────────┘
```

## Component Interaction

```
┌─────────────────────────────────────────────────────────────┐
│                     Application Layer                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Planner  →  Routing  →  Tools  →  Budget  →  Approval    │
│     │           │          │         │           │         │
│     └───────────┴──────────┴─────────┴───────────┘         │
│                           │                                 │
│                    emit_event()                             │
│                           │                                 │
└───────────────────────────┼─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                  Observability Layer                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌────────────────────────────────────────────────────┐   │
│  │         ObservabilityCollector                     │   │
│  │  ┌──────────────────────────────────────────────┐ │   │
│  │  │ Event Buffer (thread-safe, auto-flush)       │ │   │
│  │  └──────────────────────────────────────────────┘ │   │
│  │  ┌──────────────────────────────────────────────┐ │   │
│  │  │ GoldenSignals (counters, histograms)         │ │   │
│  │  └──────────────────────────────────────────────┘ │   │
│  │  ┌──────────────────────────────────────────────┐ │   │
│  │  │ Exporters (OTEL, Console, Prometheus)        │ │   │
│  │  └──────────────────────────────────────────────┘ │   │
│  └────────────────────────────────────────────────────┘   │
│                                                             │
└───────────────────────────┬─────────────────────────────────┘
                            │
                ┌───────────┴───────────┐
                │                       │
                ▼                       ▼
┌─────────────────────────┐   ┌─────────────────────────┐
│    Export Layer         │   │    Storage Layer        │
├─────────────────────────┤   ├─────────────────────────┤
│                         │   │                         │
│ OTEL Spans              │   │ Jaeger UI              │
│ OTEL Metrics            │   │ Zipkin UI              │
│ Console JSON            │   │ Prometheus TSDB        │
│ Prometheus Text         │   │ Grafana Dashboards     │
│                         │   │ Loki Logs              │
└─────────────────────────┘   └─────────────────────────┘
```

## Event Types Hierarchy

```
StructuredEvent (base)
├── event_type: EventType
├── trace_id: str
├── timestamp: float
├── attributes: dict
└── to_dict() → dict
    │
    ├── PlanEvent
    │   ├── goal: str
    │   ├── steps_count: int
    │   ├── tools_selected: list
    │   └── duration_ms: float
    │
    ├── RouteEvent
    │   ├── agent_selected: str
    │   ├── routing_policy: str
    │   ├── alternatives_considered: list
    │   └── reasoning: str
    │
    ├── ToolCallEvent
    │   ├── .start(tool_name, tool_params)
    │   ├── .complete(tool_name, duration_ms, result_size)
    │   └── .error(tool_name, error_type, error_message)
    │
    ├── BudgetEvent
    │   ├── .warning(budget_type, spent, ceiling, threshold)
    │   ├── .exceeded(budget_type, spent, ceiling, policy)
    │   └── .updated(budget_type, spent, ceiling, delta)
    │
    └── ApprovalEvent
        ├── .requested(action_description, risk_level, timeout)
        ├── .received(approved, wait_time_ms, reason)
        └── .timeout(wait_time_ms, default_action)
```

## Golden Signals Computation

```
┌──────────────────────────────────────────────────────────┐
│                    Golden Signals                        │
└──────────────────────────────────────────────────────────┘

1. SUCCESS RATE
   ┌─────────────────────────────────────┐
   │ successful_requests                 │
   │ ─────────────────────  × 100        │
   │ total_requests                      │
   └─────────────────────────────────────┘
   Target: > 95%

2. LATENCY
   ┌─────────────────────────────────────┐
   │ P50 = median(latency_samples)       │
   │ P95 = percentile(95, samples)       │
   │ P99 = percentile(99, samples)       │
   └─────────────────────────────────────┘
   Rolling window: 1000 samples
   Target: P95 < 500ms

3. TRAFFIC
   ┌─────────────────────────────────────┐
   │ total_requests                      │
   │ ──────────────────                  │
   │ elapsed_time                        │
   └─────────────────────────────────────┘
   Unit: requests/second

4. ERRORS
   ┌─────────────────────────────────────┐
   │ tool_errors                         │
   │ ────────────────  × 100             │
   │ tool_calls                          │
   └─────────────────────────────────────┘
   Target: < 5%

5. MEAN STEPS PER TASK
   ┌─────────────────────────────────────┐
   │ mean(steps_per_task_samples)        │
   └─────────────────────────────────────┘
   Target: < 10 steps

6. APPROVAL WAIT TIME
   ┌─────────────────────────────────────┐
   │ P50 = median(approval_wait_samples) │
   │ P95 = percentile(95, samples)       │
   │ P99 = percentile(99, samples)       │
   └─────────────────────────────────────┘
   Target: P95 < 30s
```

## Grafana Dashboard Layout

```
┌──────────────────────────────────────────────────────────────┐
│                  CUGAR Agent - Golden Signals                │
├────────────┬────────────┬────────────┬────────────────────────┤
│ Success    │ Tool Error │ Mean Steps │ Budget Utilization     │
│ Rate       │ Rate       │ Per Task   │ ┌──────────────────┐   │
│ ┌────────┐ │ ┌────────┐ │ ┌────────┐ │ │ Warnings:   5    │   │
│ │ 98.4%  │ │ │  2.1%  │ │ │   4.2  │ │ │ Exceeded:   2    │   │
│ │   🟢   │ │ │   🟢   │ │ │   📊   │ │ └──────────────────┘   │
│ └────────┘ │ └────────┘ │ └────────┘ │                        │
├────────────┴────────────┴────────────┴────────────────────────┤
│                      Request Rate (req/s)                      │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ ──── Total  ──── Success  ──── Failed                    │ │
│  │                                                           │ │
│  └──────────────────────────────────────────────────────────┘ │
├────────────────────────────┬───────────────────────────────────┤
│ End-to-End Latency (ms)    │ Tool Call Latency by Tool (ms)   │
│ ┌────────────────────────┐ │ ┌─────────────────────────────┐ │
│ │ ─ P50  ─ P95  ─ P99    │ │ │ ─ tool1 P95  ─ tool2 P95    │ │
│ │                        │ │ │                             │ │
│ └────────────────────────┘ │ └─────────────────────────────┘ │
├────────────────────────────┴───────────────────────────────────┤
│                    Approval Wait Time (ms)                     │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ ──── P50  ──── P95  ──── P99                             │ │
│  │                                                           │ │
│  └──────────────────────────────────────────────────────────┘ │
├──────────────┬──────────────┬──────────────────────────────────┤
│ Tool Errors  │ Tool Errors  │ Active Traces                   │
│ by Type      │ by Tool      │ ┌────────────────────────────┐ │
│ ┌──────────┐ │ ┌──────────┐ │ │         15                 │ │
│ │ 🥧 Pie   │ │ │ 🥧 Pie   │ │ │                            │ │
│ └──────────┘ │ └──────────┘ │ └────────────────────────────┘ │
├──────────────┴──────────────┴──────────────────────────────────┤
│                      Event Timeline                            │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ [12:00:01] plan_created: goal="Find flights" steps=3     │ │
│  │ [12:00:01] route_decision: agent=primary_agent           │ │
│  │ [12:00:02] tool_call_start: tool=search_flights          │ │
│  │ [12:00:02] tool_call_complete: duration=450ms            │ │
│  └──────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘
```

## Trace Propagation

```
User Request → trace_id="abc-123"
    │
    ├─→ PlannerAgent.plan()
    │       └─→ emit(PlanEvent(trace_id="abc-123", ...))
    │
    ├─→ RoutingAuthority.route()
    │       └─→ emit(RouteEvent(trace_id="abc-123", ...))
    │
    ├─→ WorkerAgent.execute_step()
    │       ├─→ emit(ToolCallEvent.start(trace_id="abc-123", ...))
    │       ├─→ execute_tool()
    │       └─→ emit(ToolCallEvent.complete(trace_id="abc-123", ...))
    │
    ├─→ BudgetEnforcer.check()
    │       └─→ emit(BudgetEvent(trace_id="abc-123", ...))
    │
    └─→ ApprovalWorkflow.request()
            ├─→ emit(ApprovalEvent.requested(trace_id="abc-123", ...))
            └─→ emit(ApprovalEvent.received(trace_id="abc-123", ...))

All events share trace_id="abc-123" → Correlation in Grafana/Jaeger
```

## Configuration Flow

```
Environment Variables
├─ OTEL_EXPORTER_OTLP_ENDPOINT
├─ OTEL_SERVICE_NAME
├─ OTEL_TRACES_EXPORTER
└─ OBSERVABILITY_ENABLED
         │
         ▼
configs/observability.yaml
├─ observability.enabled
├─ observability.auto_export
├─ observability.buffer_size
└─ observability.exporters
         │
         ▼
ObservabilityCollector(
    exporters=[...],
    auto_export=True,
    buffer_size=1000
)
         │
         ▼
Runtime Behavior
├─ Event buffering
├─ Auto-export toggle
├─ Exporter selection
└─ PII redaction
```

## Deployment Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     Production Setup                        │
└─────────────────────────────────────────────────────────────┘

┌──────────────┐
│  CUGAR Agent │
│  Container   │
│              │
│  Port 8000   │
│  /metrics    │
└──────┬───────┘
       │
       ├─────────────────┐
       │                 │
       ▼                 ▼
┌──────────────┐  ┌──────────────┐
│ OTEL         │  │ Prometheus   │
│ Collector    │  │              │
│ :4318        │  │ :9090        │
└──────┬───────┘  └──────┬───────┘
       │                 │
       ├─────────────────┤
       │                 │
       ▼                 ▼
┌──────────────┐  ┌──────────────┐
│  Jaeger UI   │  │   Grafana    │
│  :16686      │  │   :3000      │
│              │  │              │
│  Traces      │  │  Dashboards  │
│  Search      │  │  Alerts      │
└──────────────┘  └──────────────┘
```

## Security & PII Redaction

```
Event Creation
    │
    ▼
┌──────────────────────────────────┐
│ StructuredEvent(                 │
│   tool_params={                  │
│     "api_key": "secret123",      │
│     "password": "pass456",       │
│     "safe_param": "visible"      │
│   }                              │
│ )                                │
└────────────────┬─────────────────┘
                 │
                 ▼
┌──────────────────────────────────┐
│ _redact_dict() - Recursive       │
│                                  │
│ Sensitive keys:                  │
│ - secret                         │
│ - token                          │
│ - password                       │
│ - api_key                        │
│ - credential                     │
│ - auth                           │
│ - authorization                  │
│ - bearer                         │
└────────────────┬─────────────────┘
                 │
                 ▼
┌──────────────────────────────────┐
│ event.to_dict() - Redacted       │
│   tool_params={                  │
│     "api_key": "[REDACTED]",     │
│     "password": "[REDACTED]",    │
│     "safe_param": "visible"      │
│   }                              │
└──────────────────────────────────┘
                 │
                 ▼
           Export safely
```

## Thread Safety

```
Concurrent Agent Execution
    │
    ├─→ Thread 1: emit_event(PlanEvent)
    ├─→ Thread 2: emit_event(ToolCallEvent)
    └─→ Thread 3: emit_event(BudgetEvent)
             │
             ▼
┌────────────────────────────────────┐
│ ObservabilityCollector             │
│ ┌────────────────────────────────┐ │
│ │ with _buffer_lock:             │ │
│ │   _event_buffer.append(event)  │ │
│ │   if len(buffer) >= buffer_size│ │
│ │     _flush_buffer()            │ │
│ └────────────────────────────────┘ │
└────────────────────────────────────┘
             │
             ▼
        Thread-safe
```

---

**See Also**:
- [OBSERVABILITY_SLOS.md](OBSERVABILITY_SLOS.md) - Complete reference
- [INTEGRATION_CHECKLIST.md](INTEGRATION_CHECKLIST.md) - Integration guide
- [QUICK_REFERENCE.md](QUICK_REFERENCE.md) - Common patterns
